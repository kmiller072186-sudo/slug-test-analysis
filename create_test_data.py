import numpy as np\nimport pandas as pd\n\ndef generate_falling_head_data(well_parameters):\n    # Sample code to generate falling-head data based on well_parameters\n    return np.random.normal(loc=well_parameters['falling_head_mean'], scale=well_parameters['falling_head_std'], size=100)\n\ndef generate_rising_head_data(well_parameters):\n    # Sample code to generate rising-head data based on well_parameters\n    return np.random.normal(loc=well_parameters['rising_head_mean'], scale=well_parameters['rising_head_std'], size=100)\n\ndef generate_noisy_data(data, noise_level=0.1):\n    noise = np.random.normal(0, noise_level, size=data.shape)\n    return data + noise\n\ndef main():\n    well_params = {\n        'falling_head_mean': 5.0,\n        'falling_head_std': 0.5,\n        'rising_head_mean': 6.0,\n        'rising_head_std': 0.4\n    }\n    \n    falling_head = generate_falling_head_data(well_params)\n    rising_head = generate_rising_head_data(well_params)\n    noisy_data = generate_noisy_data(falling_head)\n\n    # Creating a DataFrame to store the data\n    data = pd.DataFrame({\n        'Falling_Head': falling_head,\n        'Rising_Head': rising_head,\n        'Noisy_Data': noisy_data\n    })\n\n    # Save to CSV or another format if needed\n    data.to_csv('slug_test_data.csv', index=False)\n\nif __name__ == "__main__":\n    main()\n